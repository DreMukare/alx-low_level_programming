#include "header.h"

/**
* add - adds two integers and returns the result
* @int: added to other integer
*
* Return: returns result of addition of two integers
*/
int add(int, int)
{
	int = int + int;
	return (int);
}
